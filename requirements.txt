# What the project needs, not an exact snapshot of one laptop.
# Exact pins (numpy==2.5.3 etc.) broke the clean-clone test on a machine with
# an older Python - the versions did not exist there. Loose requirements let
# pip pick versions that fit the interpreter.
anthropic
flask
pandas
pytest
python-dotenv
PyYAML
requests
reportlab
pypdf
